{
  "schemaVersion": "bounded.controlled-coding-pilot/v2",
  "pilotId": "controlled-real-coding-v2.worker-request-id-correlation",
  "profile": "bounded_text_edits",
  "contextPolicy": "task_context_selections_v1",
  "runtimeBudget": "v2_default",
  "verificationProfile": [
    "typecheck",
    "build",
    "test_smoke",
    "request_id_acceptance"
  ],
  "taskTitle": "Enforce worker request and response requestId correlation",
  "taskPrompt": "Fix the HTTP workspace worker client so that refine, infill, and resolveConflict reject an otherwise valid worker response when its requestId does not exactly match the submitted request's requestId. Matching requestIds must remain accepted. Health behavior must remain unchanged. Add a focused regression using mocked or stubbed globalThis.fetch so no real network is required. Modify only packages/worker-contract/src/index.ts and tests/smoke/contracts.ts, respect the configured patch budget, and do not change endpoint construction, request payloads, timeout behavior, response schemas, dependencies, or unrelated validation.",
  "providerRequirements": [
    "For requestId correlation regression tests, every mocked response must otherwise satisfy the full response contract for that endpoint.",
    "Test matching requestId acceptance and mismatched requestId rejection for refine, infill, and resolveConflict.",
    "A mismatch test must not pass because unrelated response fields are missing or invalid.",
    "Reuse a type-correct SharedSemanticWorkspace fixture visible in the supplied test excerpts instead of inventing a partial workspace object.",
    "Do not reference a fixture before its declaration; place regression tests after any fixture they reuse.",
    "Any global fetch mock must typecheck against the repository's TypeScript fetch type; do not rely on an unsafe direct cast that hides an incompatible mock shape."
  ],
  "sourceRevisionPolicy": "current-head",
  "allowedMutationPaths": [
    "packages/worker-contract/src/index.ts",
    "tests/smoke/contracts.ts"
  ],
  "allowedReadRoots": [
    "packages/worker-contract/src/index.ts",
    "tests/smoke/contracts.ts"
  ],
  "contextSelections": [
    {
      "path": "packages/worker-contract/src/index.ts",
      "selectors": [
        {
          "kind": "anchor",
          "start": "export type DllmWorkerMode =",
          "end": "type FetchLike = ("
        },
        {
          "kind": "anchor",
          "start": "export function createHttpWorkspaceWorkerClient(",
          "end": "async function requestJson<"
        },
        {
          "kind": "anchor",
          "start": "function assertHealthResponse(",
          "end": "function normalizeBaseUrl("
        }
      ]
    },
    {
      "path": "tests/smoke/contracts.ts",
      "selectors": [
        {
          "kind": "anchor",
          "start": "import { createHttpWorkspaceWorkerClient }",
          "end": "import { parsePolicy, starterPolicyYaml, validatePolicy }"
        },
        {
          "kind": "anchor",
          "start": "const workerClient = createHttpWorkspaceWorkerClient({",
          "end": "assert.deepEqual(auditFixtureWorkerRequest(timestampCollisionFixture), []);"
        }
      ]
    }
  ],
  "forbiddenPaths": [
    "package.json",
    "package-lock.json",
    "node_modules",
    "dist",
    "reports",
    ".github",
    "docs",
    "pilots",
    "scripts",
    "examples",
    "fixtures",
    "apps",
    "bounded-agent.policy.yml",
    ".env",
    ".git"
  ],
  "maxChangedFiles": 2,
  "maxPatchLines": 120,
  "providerCallBudget": 1,
  "retryBudget": 0,
  "requiredAssertions": [
    "refine rejects a mismatched response requestId",
    "infill rejects a mismatched response requestId",
    "resolveConflict rejects a mismatched response requestId",
    "matching response requestIds remain accepted",
    "health behavior remains unchanged",
    "regression coverage uses mocked or stubbed globalThis.fetch without real network",
    "only the two authorized files change",
    "patch stays within the configured maxPatchLines budget",
    "source worktree and GitHub remain immutable"
  ]
}
